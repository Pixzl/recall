## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] New behavior has test coverage
- [ ] User-ingested text still flows through `redact()` before being persisted
- [ ] `src/mcp/server.ts` and `src/cli/index.ts` stayed thin (retrieval logic in `Store` / `embed`)

## Notes

<!-- Anything reviewers should know -->
